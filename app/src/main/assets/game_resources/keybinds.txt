bind  ctrl+period             "audiolog_cancel"
bind  t                       "stand"
bind  shift+t                 "stand"
bind  g                       "crouch"
bind  shift+g                 "crouch"
bind  b                       "prone"
bind  shift+b                 "prone"
bind  f                       "toggle_freelook"
bind  ctrl+f                  "full_view"
bind  ctrl+d                  "normal_view"
bind  ctrl+a                  "map_view"
bind  backspace               "clear_fullscreen"
bind  ctrl+s                  "save_game"
bind  ctrl+l                  "load_game"
bind  p                       "pause"
bind  ctrl+backspace          "reload_weapon 1"
bind  alt+backspace           "reload_weapon 0"
bind  ctrl+quote              "select_grenade"
bind  ctrl+h                  "toggle_olh"
bind  ctrl+semicolon          "select_drug"
bind  ctrl+m                  "toggle_music"
bind  ctrl+q                  "quit"
bind  tab                     "cycle_weapons 1"
bind  shift+tab               "cycle_weapons -1"
bind  ctrl+1                  "cycle_detail"
bind  ctrl+g                  "toggle_opengl"
bind  alt+quote               "arm_grenade"
bind  alt+semicolon           "use_drug"
bind  alt+h                   "hud_color"
bind  alt+o                   "showhelp"
bind  1                       "bio scan"
bind  2                       "fullscreen"
bind  3                       "360 view"
bind  4                       "lantern"
bind  5                       "shield"
bind  6                       "infrared"
bind  7                       "nav unit"
bind  8                       "data reader"
bind  9                       "booster"
bind  0                       "jumpjets"
bind  f1                      "mfd left 1"
bind  f2                      "mfd left 2"
bind  f3                      "mfd left 3"
bind  f4                      "mfd left 4"
bind  f5                      "mfd left 5"
bind  f6                      "mfd right 1"
bind  f7                      "mfd right 2"
bind  f8                      "mfd right 3"
bind  f9                      "mfd right 4"
bind  f10                     "mfd right 5"
bind  0                       "keypad 0"
bind  1                       "keypad 1"
bind  2                       "keypad 2"
bind  3                       "keypad 3"
bind  4                       "keypad 4"
bind  5                       "keypad 5"
bind  6                       "keypad 6"
bind  7                       "keypad 7"
bind  8                       "keypad 8"
bind  9                       "keypad 9"
bind  escape                  "toggle_options"
bind  ctrl+2                  "cheat_give_all"
bind  ctrl+3                  "cheat_physics"
bind  ctrl+4                  "cheat_up_level"
bind  ctrl+5                  "cheat_down_level"

bind  enter                   "fire"

bind  shift+w                 "runforward"
bind  shift+up                "runforward"
bind  alt+up                  "runforward"
bind  shift+keypad_up         "runforward"
bind  alt+keypad_up           "runforward"
bind  w                       "forward"
bind  up                      "forward"
bind  keypad_up               "forward"
bind  shift+z                 "fastturnleft"
bind  shift+left              "fastturnleft"
bind  shift+keypad_left       "fastturnleft"
bind  z                       "turnleft"
bind  left                    "turnleft"
bind  keypad_left             "turnleft"
bind  shift+c                 "fastturnright"
bind  shift+right             "fastturnright"
bind  shift+keypad_right      "fastturnright"
bind  c                       "turnright"
bind  right                   "turnright"
bind  keypad_right            "turnright"
bind  s                       "back"
bind  shift+s                 "back"
bind  down                    "back"
bind  shift+down              "back"
bind  alt+down                "back"
bind  keypad_down             "back"
bind  shift+keypad_down       "back"
bind  alt+keypad_down         "back"
bind  a                       "slideleft"
bind  shift+a                 "slideleft"
bind  alt+left                "slideleft"
bind  alt+keypad_left         "slideleft"
bind  keypad_end              "slideleft"
bind  d                       "slideright"
bind  shift+d                 "slideright"
bind  alt+right               "slideright"
bind  alt+keypad_right        "slideright"
bind  keypad_pgdn             "slideright"
bind  j                       "jump"
bind  shift+j                 "jump"
bind  space                   "jump"
bind  shift+space             "jump"
bind  ctrl+space              "jump"
bind  alt+space               "jump"
bind  x                       "leanup"
bind  shift+x                 "leanup"
bind  ctrl+x                  "leanup"
bind  alt+x                   "leanup"
bind  q                       "leanleft"
bind  shift+q                 "leanleft"
bind  ctrl+left               "leanleft"
bind  ctrl+keypad_left        "leanleft"
bind  e                       "leanright"
bind  shift+e                 "leanright"
bind  ctrl+right              "leanright"
bind  ctrl+keypad_right       "leanright"
bind  r                       "lookup"
bind  shift+r                 "lookup"
bind  ctrl+r                  "lookup"
bind  ctrl+up                 "lookup"
bind  ctrl+keypad_up          "lookup"
bind  v                       "lookdown"
bind  shift+v                 "lookdown"
bind  ctrl+v                  "lookdown"
bind  ctrl+down               "lookdown"
bind  ctrl+keypad_down        "lookdown"
bind  keypad_home             "runleft"
bind  keypad_pgup             "runright"
bind  s                       "thrust"
bind  shift+s                 "thrust"
bind  keypad_5                "thrust"
bind  w                       "climb"
bind  shift+w                 "climb"
bind  up                      "climb"
bind  shift+up                "climb"
bind  ctrl+up                 "climb"
bind  alt+up                  "climb"
bind  keypad_up               "climb"
bind  shift+keypad_up         "climb"
bind  ctrl+keypad_up          "climb"
bind  alt+keypad_up           "climb"
bind  a                       "bankleft"
bind  shift+a                 "bankleft"
bind  keypad_left             "bankleft"
bind  shift+keypad_left       "bankleft"
bind  ctrl+keypad_left        "bankleft"
bind  alt+keypad_left         "bankleft"
bind  d                       "bankright"
bind  shift+d                 "bankright"
bind  keypad_right            "bankright"
bind  shift+keypad_right      "bankright"
bind  ctrl+keypad_right       "bankright"
bind  alt+keypad_right        "bankright"
bind  x                       "dive"
bind  shift+x                 "dive"
bind  down                    "dive"
bind  shift+down              "dive"
bind  ctrl+down               "dive"
bind  alt+down                "dive"
bind  keypad_down             "dive"
bind  shift+keypad_down       "dive"
bind  ctrl+keypad_down        "dive"
bind  alt+keypad_down         "dive"
bind  q                       "rollright"
bind  shift+q                 "rollright"
bind  z                       "rollright"
bind  shift+z                 "rollright"
bind  e                       "rollleft"
bind  shift+e                 "rollleft"
bind  c                       "rollleft"
bind  shift+c                 "rollleft"
bind  keypad_home             "climbleft"
bind  shift+keypad_home       "climbleft"
bind  ctrl+keypad_home        "climbleft"
bind  alt+keypad_home         "climbleft"
bind  keypad_pgup             "climbright"
bind  shift+keypad_pgup       "climbright"
bind  ctrl+keypad_pgup        "climbright"
bind  alt+keypad_pgup         "climbright"
bind  keypad_pgdn             "diveright"
bind  shift+keypad_pgdn       "diveright"
bind  ctrl+keypad_pgdn        "diveright"
bind  alt+keypad_pgdn         "diveright"
bind  keypad_end              "diveleft"
bind  shift+keypad_end        "diveleft"
bind  ctrl+keypad_end         "diveleft"
bind  alt+keypad_end          "diveleft"
